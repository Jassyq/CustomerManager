# Customer Price Manager v4.0 - Required Python Packages
# Install these packages with: pip install -r requirements.txt

# Core dependencies - stable versions without build issues
streamlit==1.28.1
openpyxl==3.10.10
pandas==2.0.3

# No pyarrow - not needed and causes build failures on new computers
# Optional but recommended for better performance
xlrd==2.0.1
Pillow==10.0.0